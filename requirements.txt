langgraph>=0.2.60
langchain>=0.3.14
langchain-anthropic>=0.3.4
langchain-groq>=0.2.3
langchain-community>=0.3.14
langchain-chroma>=0.2.0
langchain-text-splitters>=0.3.4
langchain-core>=0.3.29

# Embeddings run on ONNX Runtime, which chromadb bundles. Deliberately NOT
# sentence-transformers / langchain-huggingface: those pull in torch, which costs
# ~190 MB of resident memory before a model loads and pushes the process past the
# 512 MB free-tier limit. See rag/ingest.py OnnxMiniLMEmbeddings.
chromadb>=0.5.23

pydantic>=2.9.0
python-dotenv>=1.0.1
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
aiofiles>=24.1.0
pypdf>=5.1.0
pytest>=8.3.4
